/**
 * Home flow barrel — the Today surface (design scr-core.jsx Today).
 *
 * The screen reads the shared snapshot (state/appData.ts); all derivation
 * lives in engine/insights.ts. Nothing else is exported from this flow.
 */

export { HomeFlow } from "./HomeFlow.js";
